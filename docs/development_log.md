# Development Log

## 2026-06-29
(word_to_id 구현)

- word_to_id에 특수문자 처리를 위한 정규표현식을 배웠다.
- 특수문자가 하나 있을 때와 여러 개 있을 때의 의미가 다를 수 있다는 것을 알았다. 이 부분은 나중에 처리해 봐야 할 것 같다.

## 2026-07-01
(corpus 생성 및 max_length 후보군 생성)

- coverage를 이용해 적절한 토큰 시퀀스의 최대 길이를 결정하는 방법을 배웠다.
- PyTorch에서는 TensorFlow처럼 max_length를 지정하는 방식없어서 직접 padding을 구현해야 한다.
- 요즘은 배치별로 padding을 해서 max_length를 지정하지 않음. 메모리 절약과 연산 속도에 이점이 있다.
- 배치별 padding 기술을 통해 모델의 입력에 시계열 데이터의 길이가 의미가 없다는 것을 깨달았다.
- 그리고 이 깨달음으로, 머릿속으로 모델 설계를 러프하게 했다고 생각했지만 오만이었다는 것을 알았다. 제대로 모델 설계를 먼저 했다면 출력 구조를 고민하게 되었을테고 공부했던 내용을 다시 복습하며 이런 사실을 미리 알았을 것이다. 또한 이번 프로젝트에서는 문장 하나당 라벨이므로 마지막 hidden state만 이용하면 된다는 것도 알았다. 이것도 사전에 모델 설계를 했다면 알았을 내용이다.
- 앞으로는 모델 구조와 Tensor shape를 먼저 설계한 후 구현을 시작해야겠다.

## 2026-07-10 (모델 설계 및 실험 계획 작성, word_to_id 희소 빈도 처리 구현)

- 파이토치에서는 Affine을 Linear라고 부른다는 것을 배웠다.
- 모델을 설계할 때 몇 층으로 쌓아야 하는지, 하이퍼파라미터를 몇으로 해야 하는지 등을 명확한 공식으로 결정하는 것이 아니라는 점 때문에 책 『파이썬 딥러닝 머신러닝 입문』의 내용을 거의 그대로 가져왔다. 이 점이 만족스럽지 않았다. 실제 연구에서는 어떤 기준으로 이러한 값들을 결정하는지 궁금해졌다.
- 원래는 word_to_id를 만들 때 희소 빈도 처리를 하지 않는 것을 기본 모델로 하려고 했지만 그러면 사전의 크기가 306,937로 너무 커져서 희소 빈도 처리를 하기로 했다. 사전의 크기가 모델의 메모리 사용량과 계산량에 직접적인 영향을 준다는 점을 깨달으면서 모델 설계가 왜 중요한지 다시 실감했다.
- 모델 설계를 마치니 앞으로의 목표가 명확해져서 프로젝트를 진행하기 수월해질 것이라는 확신이 들었다. 이를 통해 모델 설계가 프로젝트의 방향을 정하는 데 중요한 과정이라는 점을 느낄 수 있었다.

## 2026-07-11
(실험 계획 수정, 패딩 구현, 검증 데이터 분리, venv 가상환경 생성)

- 전처리 실험 계획을 다시 검토했다. 처음에는 연속 특수문자(`..`, `^^`, `,,` 등)를 별도로 처리하면 모델의 학습 성능이 향상될 것이라고 생각하여 실험 항목에 포함했었다. 그러나 RNN 계열 시퀀스 모델은 이전 입력과 문맥을 바탕으로 현재 입력을 해석하므로, 이러한 패턴을 모델이 스스로 학습할 가능성이 있으며 사람이 규칙 기반 전처리를 수행할 필요가 없을 수도 있다는 점을 깨달았다. 다만 실제로 전처리의 효과가 어느 정도인지 확인할 필요가 있다고 판단하여 해당 실험은 유지하기로 했다.
- 위의 생각을 바탕으로 전처리 실험 계획을 전반적으로 재정리했다. 기본 전처리에서는 문장 부호 및 특수문자를 공백으로 분리하고, 감성 표현 전처리에서는 연속 특수문자와 반복 자모(ㅋ, ㅎ, ㅠ, ㅜ )를 처리하도록 역할을 명확히 구분했다.
- word_to_id를 만들기 전에 validation dataset을 분리했어야 했는데 이걸 이제야 깨달았다. 앞으론 조심하자.
- 파이썬 라이브러리 설치 과정에서 패키지 버전 충돌이 발생했다. 이로인해 프로젝트별 독립적인 패키지 환경 관리의 필요성을 이해했고, venv 가상환경 사용법을 익혔다.

## 2026-07-12
(전처리 결과 pickle 저장 및 불러오기 구현, majority baseline 구현)

## 2026-07-13
(lstm 모델 구현, 모델의 학습 및 검증 코드 구현, 모델 체크포인트 저장 및 불러오기 구현)

```
Epoch 1/10 | Train Loss: 0.5480 | Valid Loss: 0.5007
Epoch 2/10 | Train Loss: 0.4320 | Valid Loss: 0.4864
Epoch 3/10 | Train Loss: 0.3623 | Valid Loss: 0.5207
Epoch 4/10 | Train Loss: 0.2998 | Valid Loss: 0.5532
Epoch 5/10 | Train Loss: 0.2431 | Valid Loss: 0.6302
Epoch 6/10 | Train Loss: 0.1981 | Valid Loss: 0.7474
Epoch 7/10 | Train Loss: 0.1648 | Valid Loss: 0.8790
Epoch 8/10 | Train Loss: 0.1411 | Valid Loss: 1.0029
Epoch 9/10 | Train Loss: 0.1249 | Valid Loss: 1.1229
Epoch 10/10 | Train Loss: 0.1129 | Valid Loss: 1.2035

Test Accuracy: 0.7341
```

- 책에서는 감성분석 AI를 텐서플로우로 만드는 방법을 알려줬었다. 하지만 텐서플로우 대신에, 파이토치로 모델을 직접 클래스로 구현하면서 프레임워크의 구조를 더 깊게 이해할 수 있었다. 이 방법이 귀찮지만 더 재밌었다. 또한 파이토치의 높은 자유도를 체감할 수 있었다.
- 초기 하이퍼파라미터로 10에포크 돌려봤을 때, 2에포크 이후부터 과적합을 보였다. 나중에 embedding_dim과 hidden_size를 개선할 필요가 있어보인다.
- 파이토치의 autograd에 대한 간단한 지식을 얻었다. 연산 과정에서 동적으로 계산 그래프가 생성되고, backward() 호출 시 자동 미분이 수행되는 구조가 인상적이었다.

## 2026-07-14
(하이퍼파라미터, max_length 튜닝)

| embedding_dim | hidden_size | Test Accuracy |
|-|-|-|
|           128 |          64 |        0.7362 |
|            64 |          64 |        0.7365 |
|           128 |          32 |        0.7357 |
|            64 |          32 |        0.7368 |
|        **32** |      **32** |    **0.7444** |
|        **64** |      **16** |    **0.7402** |
|        **32** |      **16** |    **0.7481** |

- 하이퍼파라미터 탐색 결과, embedding_dim=32, hidden_size=16에서 가장 높은 테스트 정확도(0.7481)을 기록하였다.
- 다만 모델 간(RNN, LSTM, GRU, Bi-LSTM) 비교 실험에서는 과도한 하이퍼파라미터 최적화를 피하고 공정한 비교를 위해 hidden_size를 32로 결정하였다.
- 또한 hidden_size를 64에서 32로 줄여도 성능 저하가 거의 없었으며, 모델 크기와 연산량을 줄일 수 있다는 장점도 있었다.

| Length | Coverage
|-:|-:|
| 5 | 0.333291 |
| 7 | 0.491830 |
| 12 | 0.757219 |
| 16 | 0.853002 |
| 21 | 0.904382 |

| max_length | Best Epoch | Best Valid Loss | Test Accuracy |
|-:|-:|-:|-:|
| 5 | 4 | 0.4921 | 0.7444 |
| 7 | 3 | 0.4576 | 0.7642 |
| 12 | 4 | 0.4184 | 0.7905 |
| 16 | 4 | 0.4047 | 0.8026 |
| 21 | 5 | 0.4080 | 0.8046 |

- max_length는 탐색 결과 21일 때 가장 높은 테스트 정확도(0.8046)를 기록했다.
- 가장 많은 빈도를 기록한 길이인 5가 최적의 성능일 것이라고 예상했지만, 실제로는 더 긴 max_length에서 성능이 향상되었다. 이번 실험을 통해 최빈 길이보다 충분한 토큰 정보를 보존하는 것이 성능에 더 중요할 수 있다는 것을 알게 되었다.

## 2026-07-15
(전처리 실험 계획 관련 오류 수정, 문장 토큰화 모듈 생성)

- 생각해보니 토큰화 방식에 따라서 문장의 길이 분포가 달라지므로, 전처리 실험에서 max_length를 하나로 통일하는 것은 옳지 않은 방식이었다. 실험 계획이 완벽했다고 생각했는데 여전히 빈틈이 많았다.
- 따라서 이후 실험에서는 max_length 대신 동일한 정보 보존률을 의미하는 coverage를 고정 조건으로 사용하기로 했다.
- 기본 전처리에서 90% coverage에 해당하는 길이에서 가장 높은 테스트 정확도를 기록했기 때문에, 각 전처리 방식의 max_length는 90% coverage를 만족하는 값으로 설정하기로 했다.
- 편한 실험 환경을 만들기 위해 문장 토큰화 과정을 모듈화했다.
- tokenizer 설정을 config에서 변경할 수 있도록 구성하여, 실험마다 코드를 직접 수정하지 않고 설정값만 바꾸는 방식으로 개선했다.
- 토크나이저 교체와 비교 실험이 쉬워져 이후 다양한 토큰화 방식(character-level, subword 등)을 테스트하기 편해졌다.

## 2026-07-16
(실험 1 완료)

| Tokenizer | vocab_size | max_length | Best Epoch | Best Valid Loss | Test Accuracy |
|:-|-:|-:|-:|-:|-:|
| Basic | 39,510 | 21 | 5 | 0.4080 | 0.8046 |
| Sentiment | 39520 | 19 | 3 | 0.4064 | 0.8016 |
| Character-level | 2,939 | 75 | 9 | 0.3478 | 0.8447 |
| Morpheme | 21,449 | 39 | 4 | 0.3266 | 0.8546 |
| Subword | 8,192 | 37 | 7 | 0.3400 | 0.8426 |

- 감성 분석 전처리는 그 효과가 거의 없었다. 오히려 기본 전처리가 작은 차이이긴 해도 더 높은 성능을 보인 점이 신기한 부분이었다. 감성 표현을 전처리하면서 일부 의미 있는 정보를 손실시킨 것으로 예상이 된다.
- character-level 방식이 단순 단어 단위 토큰화보다 정확도가 더 높다는게 매우 놀라웠다. 문자 단위의 입력값과 이전 시각의 입력을 바탕으로 긍정/부정을 도출할 수 있다는 점에서 놀라웠다.
- subword라는 것을 처음 배웠을 때, 이 방식이 가장 성능이 좋을 것이라 예상했다. 최신 기술인 것처럼 보여서 특출난 성능을 가졌고, 현재의 데이터로부터 학습하는 것이기 때문에 이번 문제에 특화되어 더 뛰어난 성능을 보여줄 것이라고 생각했기 때문이다. 하지만 형태소와 문자 단위 토큰화보다 정확도가 낮은 결과를 보였다.
- 그럼에도 subword 방식이 유의미한 방식인 점을 다른 부분에서 알 수 있었다. max_length가 문자 단위의 거의 절반인데도 비슷한 정확도를 보였다는 것이다. 즉, 메모리 효율과 속도면에서 좋은 기술이라는 것을 잘 알 수 있었다.

### 추가 실험
- SentencePiece의 vocab size를 8k로 설정했었다. 일반적으로 2의 제곱 크기(8k, 16k, 32k 등)를 사용하는 경우가 많으며, Character-level보다 크고 Word-level 및 형태소 분석보다 작은 적절한 시작점이라고 판단한 이유에서다.
- 8k와 16k 중에 고민했는데, 일단 8k로 진행하기로 했다. 실험 1 결과를 확인하고 필요시 vocab size에 따른 성능 변화에 대한 간단한 실험을 하자고 생각했다. 실험 1의 결과를 보고 아래와 같은 이유로 추가 실험을 진행했다.
- subword가 Character-level과 형태소 단위 보다 성능이 낮게 나와 아쉬움이 들었다. 형태소의 vocab size가 21k이기 때문에 subword의 vocab size를 8k가 아닌 16k로 더 키운다면 의미가 더 잘 보존되어 성능이 향상될 가능성이 있다는 생각이 들었다. 따라서 실험해본 그 결과는 아래와 같다.

| Tokenizer | vocab_size | max_length | Best Epoch | Best Valid Loss | Test Accuracy |
|:-|-:|-:|-:|-:|-:|
| Subword | 8,192 | 37 | 7 | 0.3400 | 0.8426 |
| Subword | 16,384 | 32 | 3 | 0.3483 | 0.8404 |

- vocab size를 16k로 늘렸음에도 성능 향상은 나타나지 않았다. 오히려 검증 손실이 더 빨리 증가하는 모습을 보여 과적합이 더 빠르게 발생하는 경향을 확인하였다.
- 이런 결과를 보면 현재 데이터와 모델에서는 8k 정도의 vocab size만으로도 충분했을 것이라고 생각한다.


## 2026-07-17
(RNN 구현, best_valid_loss 변수 버그 수정 및 실험 1 재수행)

- RNN 모델은 LSTM 모델과 달리 MPS에 최적화가 되어있지 않아서 MPS를 쓰는 것이 CPU를 썼을 때보다 오히려 학습 시간이 느려졌다.

- 실험 1의 결과가 잘 못 됐다는 사실을 확인했다. Model checkpointing 구현 과정에서 버그가 있었다. 원래는 가장 작은 validation loss를 기록한 모델의 가중치를 저장해야 했지만, 코드 오류로 인해 마지막 epoch(10 epoch)의 모델이 저장되었다. 이에 버그를 수정한 후 실험 1을 다시 수행하였다.

| Tokenizer | vocab_size | max_length | Best Epoch | Best Valid Loss | Test Accuracy |
|:-|-:|-:|-:|-:|-:|
| Basic | 39,510 | 21 | 4 | 0.4026 | 0.8078 |
| Sentiment | 39520 | 19 | 5 | 0.4084 | 0.8092 |
| Character-level | 2,939 | 75 | 9 | 0.3478 | 0.8471 |
| Morpheme | 21,449 | 39 | 7 | 0.3276 | 0.8578 |
| Subword | 8,192 | 37 | 6 | 0.3395 | 0.8488 |

- 이전 결과에서는 감성 분석이 더 낮은 정확도를 보였는데 이번 결과에서는 더 높은 정확도를 보였다. 하지만 차이가 0.14%p로 큰 차이를 보이지 않았다. 이로써 감성 분석 전처리의 효과가 없거나 아주 미미한 수준인 것으로 판단된다.
- Character-level도 이전 실험 결과에서는 Subword보다 더 높은 정확도를 보였지만, 이번에는 더 낮은 정확도를 보였다. Subword가 Character-level보다 우수한 성능이라고 결론 내리기는 어렵지만, 더 짧은 시퀀스 길이에서 비슷한 수준의 성능을 보였다는 점에서 효율적인 토큰화 방식임을 다시 확인할 수 있었다.

### 추가 실험
- 추가 실험 또한 다시 진행해보았다.

| Tokenizer | vocab_size | max_length | Best Epoch | Best Valid Loss | Test Accuracy |
|:-|-:|-:|-:|-:|-:|
| Subword | 8,192 | 37 | 6 | 0.3395 | 0.8488 |
| Subword | 16,384 | 32 | 4 | 0.3406 | 0.8485 |

- vocab size를 16k로 늘렸음에도 성능 향상은 나타나지 않았고, 거의 동일한 정확도를 보였다.
- 이전 실험과의 결론에서 변동은 없었다.
- vocab size를 16k로 늘렸음에도 성능은 거의 동일하였다. 따라서 현재 데이터셋과 모델에서는 8k vocabulary만으로도 충분한 성능을 얻을 수 있음을 다시 확인하였다.


## 2026-07-18
(loss/acc 그래프 visualization 구현)

## 2026-07-21
(Confusion Matrix 구현, f1-score 구현, gru 구현, bilstm 구현)

- NumPy는 CPU에서만 연산이 가능하다. 따라서 GPU에서 계산한 Tensor는 `.cpu()`로 CPU 메모리로 옮긴 후 사용해야 한다.
- PyTorch Tensor는 GPU 메모리에 있을 수 있으므로 바로 NumPy 배열로 변환하지 말고 이를 고려해야 한다. 만약 GPU 메모리에 있다면 `tensor.cpu().numpy()` 순서로 변환해야 한다.
- PyTorch의 LSTM은 `bidirectional=True` 옵션으로 BiLSTM을 지원한다. 연결, 합, 평균 방식 중에 연결 방식을 이용한다. 합(sum)이나 평균(mean) 방식이 필요하다면 직접 구현해야 한다.
- BiLSTM의 `h_n` 형상은 `(num_layers × num_directions, batch_size, hidden_size)`이다. 예를 들어 2층 BiLSTM이라면 `(4, batch_size, hidden_size)`가 되며, 순서는 `1층 forward → 1층 backward → 2층 forward → 2층 backward`이다.


## 2026-07-22
(history.pkl을 json으로 변환, 실험 2 종료)

- 실험 결과를 python으로 출력하지 않아도 쉽게 접근할 수 있도록 json 형식으로 변환했다.